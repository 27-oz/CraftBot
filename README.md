# CraftBot V6
A modular Discord bot featuring Hybrid Commands (Slash + Prefix fallback).

## Features
- **Auto-Sync**: Automatically registers command tree on startup.
- **Secure**: Uses environment variables for tokens.

## Deployment
Deployed via Render with automatic GitHub synchronization.
